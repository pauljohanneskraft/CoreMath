branches:
  only:
  - master
  - Development
  - travis-ci-on-linux
os:
- linux
- osx
language: generic
sudo: required
dist: trusty
osx_image: xcode8
install:
  - gem install xcpretty
script:
  - xcodebuild test -workspace Math.xcworkspace -scheme Math | xcpretty
