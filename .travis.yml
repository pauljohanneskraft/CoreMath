branches:
  only:
  - master
  - Development
  - travis-ci-on-linux
language: objective-c
os:
- linux
- osx
osx_image: xcode8
install:
  - gem install xcpretty
script:
  - xcodebuild test -workspace Math.xcworkspace -scheme Math | xcpretty
